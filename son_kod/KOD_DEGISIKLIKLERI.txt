================================================================
GİRDAP İDA — son_kod DEĞİŞİKLİK ÖZETİ (14 Temmuz 2026)
================================================================
Bu klasör, girdap-video reposunun (14.07 zip) ve dünkü B1/B2
fix'li girdap-decision kopyasının 3-yönlü git birleşimidir.
Doğrulama: 267 test geçti / 0 hata (e2e video senaryosu dahil).

----------------------------------------------------------------
1) VIDEO REPOSUNA GÖRE EKLENENLER (dünkü B1/B2 fix'leri + bugün)
----------------------------------------------------------------
[B1 — mod savaşı fix'i]
- config/hardware.yaml: AUTO video bloğu eklendi:
    fsm.start_on_mode: "AUTO"     (QGC'den AUTO = görev başlar)
    bridge.auto_guided: false     (köprü modu GUIDED'a ZORLAMAZ;
                                   yoksa FC AUTO görevinden atılır)

[B2 — Ekran-2 sahte veri fix'i]
- telemetry_node.py (+95 satır): setpoint_source parametresi:
    "girdap" = GUIDED/MPPI (yarışma varsayılanı, davranış aynı)
    "fc"     = AUTO video: thrust /mavros/rc/out PWM'inden
               (% normalize), MPPI thrust'ı YOK SAYILIR,
               hiz_setpoint görev aktifken fc_cruise_setpoint_mps
- config/hardware.yaml: telemetry.setpoint_source: "fc",
    fc_cruise_setpoint_mps: 1.0 (= WP_SPEED; Yahya duruma göre
    değiştirir — değişince FC paramı + ground_speed_publisher.py
    ile SENKRON tutulmalı!), fc_thrust_left_ch: 1 / right_ch: 3
- config/params.yaml: aynı parametrelerin yarışma varsayılanları
- csv_logger.py (+22): pwm_to_thrust_pct() (1500±500 → ±%100)
- hardware.launch.py (+50): yeni telemetry parametrelerinin
  launch kablolaması
- test_telemetry_node.py (+73): fc modu testi (rc/out'tan thrust,
  MPPI yok sayma, cruise setpoint)
- test_telemetry_logger.py (+34): pwm_to_thrust_pct testleri

[Doc düzeltmeleri — bugün]
- 8 doc/kod dosyasında RFD868 → MicoAir (gerçek telemetri modülü);
  "868 MHz" iddiaları "frekans teyidi BEKLİYOR" oldu
  (2.4 GHz çıkarsa şartname md 4.1 yasak!).
  Tarihli günlükler (donanim/pc_gunlugu) bilerek DOKUNULMADI.

----------------------------------------------------------------
2) DECISION(2) KOPYASINA GÖRE EKLENENLER (video reposundan gelen)
----------------------------------------------------------------
- scripts/girdap-karar.service: Jetson systemd servisi — güç ver,
  yığın kendiliğinden kalkar (mission_source=fc, ROS_DOMAIN_ID=42,
  Restart=on-failure). Kurulum 3 komutu dosyanın başında.
- mavros_bridge_node.py (F-M.3): FSM KILL'e düşünce FCU da disarm
  edilir (KILL latch güvenlik yaması).
- prototype/tests/test_video_e2e.py: uçtan uca video senaryosu
  testi (sahte QGC+FCU+GPS ile fsm+mission_manager+planning+
  telemetry birlikte).
- prototype/tests/test_hardware_launch_config.py: launch config
  doğrulama testi.
- hardware.launch.py (F-V.5): config fallback'i artık SESSİZ değil
  (yazım hatası video bayraklarını kaybettirirse operatör görür).
- docs/hata_defteri.md + kök dizinde test-plani.md, testler/,
  kontrol-listesi.md.
- planning_node: test enjeksiyonu (parameter_overrides passthrough);
  mission_manager + video_mission.yaml: F-V.4 doc netleştirmesi
  (4 nokta, dönüş MANUEL).

----------------------------------------------------------------
3) BİRLEŞİM SIRASINDA YAPILAN EK DÜZELTMELER (bugün)
----------------------------------------------------------------
- test_telemetry_node.py çakışması: iki tarafın eklediği İKİ test
  de tutuldu (fc-thrust testi + görev-dışı-boş-setpoint testi).
- test_video_e2e.py: importorskip kapısı eklendi — yoksa ROS'suz
  ortamda (CI dahil) TÜM test koleksiyonu kırılıyordu.
- girdap-karar.service yorumu: GUIDED → AUTO (gerçek video modu).

----------------------------------------------------------------
4) YAN DOSYALAR (repo dışı, canlı izleme/test)
----------------------------------------------------------------
- ground_speed_publisher.py: WP_SPEED 5.0 → 1.0; görev dışında
  setpoint artık YAYINLANMIYOR (0 çizgisi yerine grafikte boşluk).
- plotjuggler_girdap.xml: gereksiz "return 5.0" snippet'i silindi;
  AUTO'da boş kalan cmd_vel + target_attitude abonelikten çıkarıldı.
- fake_mavros_publisher.py (YENİ): Pixhawk/MAVROS olmadan masa
  testi için sahte veri üretici (dikdörtgen tur senaryosu).
  UYARI: gerçek MAVROS çalışırken ASLA açma (aynı topic'lere yazar).

----------------------------------------------------------------
5) SÜRÜCÜ ENTEGRASYONU (14 Tem akşam — son_kod artık TAM bağımsız)
----------------------------------------------------------------
- ida_topics paketi (IDA_GIT'in 13 Tem doğrulanmış hali) repoya alındı:
  karar/ros2_ws/src/ida_topics_yeni — Livox UDP sürücüsü (SDK'sız,
  doğrulanmış IP 192.168.117.100/port 56301), OAK-D depthai sürücüsü,
  gps_imu MAVROS köprüsü, kamera_kayit (= ŞARTNAME DOSYA-1 mp4 üreticisi!)
- setup.py: 6 eksik entry point eklendi (sürücüler ros2 run ile çağrılabilir)
- hardware.launch.py: with_drivers:=true bayrağı — Livox+OAK+kamera_kayit
  remap'lerle başlar (/lidar/points→/livox/lidar, /camera/image_raw→
  /oak/rgb/image_raw). Video günü varsayılan KAPALI (MAVROS yeter).
- kamera_kayit: /tmp/kamera → ~/girdap_logs/kamera (reboot'ta Dosya-1
  kaybı = 5 ceza riski kapandı) + output_dir parametresi
- 4 sürücü main'ine KeyboardInterrupt temiz kapanışı
- "Sürücüler başka arkadaşta" notları güncellendi (hardware.yaml,
  launch, CLAUDE.md)
CANLI TESTLER: Livox sahte UDP → PointCloud2 birebir (x=5.150 ✓) ·
OAK cihazsız zarif hata + ayakta ✓ · kamera_kayit 7.6 sn geçerli MP4 ✓ ·
gps_imu köprüsü fake_mavros ile ✓ · tam paket 269 test ✓

----------------------------------------------------------------
6) EYÜP BULGULARININ UYGULANMASI (14 Tem gece — kendi kodumuzla)
----------------------------------------------------------------
Eyüp'ün AUTO denetimindeki 5 kusur son_kod'da bağımsız düzeltildi
(onun kodu yalnız sorunları görmek için okundu):
- F-M.6: köprü FC bağlantı kenarında 10 Hz akış ister (taze FC 1 Hz
  yayınlar → Ekran-2 basamaklı olurdu). stream_rate_hz param.
- F-V.6 (VİDEO-KATİL): AUTO-önce-ARM akışında görev başlamıyordu →
  fsm start_on_arm_in_mode (video true / yarışma false).
- F-V.7: AUTO'da sahte dwell (dwell_time_s: 0.0) + waypoint üstünde
  açı savrulması (yaw_setpoint_min_dist_m: 0.5).
- F-V.8: görev ilerlemesi FC'nin /mavros/mission/reached'i ile İLERİ
  senkron (bilerek volatile QoS + IDLE koruması).
- F-P.1: bayat odom'la kör sürüş → odom_timeout_s 1.0, thrust sıfır.
- mission_source varsayılanı "fc" (elle launch tuzağı kapandı).
Doğrulama: suite 276/4 (7 yeni çekirdek testi). KALAN: node-düzeyi
testler + video_testleri.sh koşumu (memory/kod-duzeltme.md'de liste).

----------------------------------------------------------------
7) VİDEO HAZIRLIK TURU (14 Tem gece 2 — sıfırdan kontrol + birleşim)
----------------------------------------------------------------
"Testler kötü çıktı" alarmı YANLIŞ ALARMDI: ros2_ws/install/setup.bash
source'lanmadan koşunca ROS node testleri sessizce SKIP oluyor (227/15
görünür). Doğru ortamda gerileme YOK. README'ye build+test reçetesi yazıldı.

[son_kodv2/Eyüp bulguları bizde de vardı — kendi kodumuzla kapatıldı]
- F-M.7: boot bekçisi ilk STATE MESAJINDA değil ilk connected=TRUE'da
  kurulur (_ever_connected). "1 mesaj + sessizlik" boot penceresi artık
  sahte KILL latch'i üretmez. 2 test.
- F-S.8: gerçek IMU darbesi parkur katmanına bağlandı (confirm_impact,
  yalnız PARKUR_3'te) — yoksa /girdap/parkur/state sonsuza dek P3'te
  kalırdı. 2 test.
- F-S.5: disk-dolu koruması — csv_logger.write_sample bool döner,
  local_map.write_frame None döner (+bozuk grid'e net ValueError); node'lar
  loglayıp örneği atlar; ida_topics telemetri (+fsync) ve local_map aynı
  desen. ida_topics telemetri /tmp → ~/girdap_logs/telemetri (Dosya-2
  reboot kaybı kapandı). 3 test.
- 8547002 portu: ida_topics perception_node /perception/objects artık TÜM
  tespitleri yayınlar; control_node time.sleep(1.0) executor-bloklaması
  future-callback zincirine çevrildi.
- F-P.1 hizası: odom HİÇ gelmediyse bayat sayılmaz (boot yanlış alarmı);
  Eyüp tarafının 3 node testi (test_planning_node.py) alındı, bizde geçiyor.

[girdap-video-main (2).zip birleşimi — onların gece deltası]
- Kod fix'leri zaten bizde bağımsız vardı → KENDİ uygulamalarımız korundu.
- fcu_url ttyACM0 → ttyUSB0 (F-M.9: USB-C soketi arızalı, Eyüp kararı;
  zip'te bile henüz yoktu — Eyüp memory'sinden alındı; udev DU0EFEA7 notu).
- hardware.yaml mission.arrival_radius_m: 2.0 (FC WP_RADIUS senkron notu)
  + launch kablolaması; params.yaml'a yeni parametrelerin yarışma
  varsayılanları (start_on_arm_in_mode false, stream_rate_hz, odom_timeout_s,
  yaw_setpoint_min_dist_m) belgeli eklendi.
- run_ekran2 --thrust-birim {%,N} (fc modunda eksen % yazar; varsayılan
  birimsiz). 1 test. docs/hata_defteri.md + video_gunu_runbook.md (§0-A
  AUTO dönüşü) onların güncel haliyle birleştirildi (3-yönlü, temiz).
- README.md SIFIRDAN: son_kod kimliği + video akışı + build/test reçetesi
  + sahte-veri masa testi + senkron kuralları.

[Masa testi altyapısı — YENİ]
- hardware.launch with_mavros:=false (FC yokken mavros başlatılmaz; sahte
  yayıncıyla /mavros/state yarışması = sahte disarm/KILL önlenir).
- fake_mavros_publisher: NavSatFix eklendi (konum entegrasyonu) → görev
  ilerleyişi artık masa testinde GERÇEK.
- config/masa_test_mission.yaml: sahte turla birebir hizalı 4 köşe.

DOĞRULAMA (bu tur):
- Tam suite: 287 passed / 4 skipped (yeni taban; önceki 276).
- CANLI sahte-veri koşusu (ROS_DOMAIN_ID=77, 155 sn, 0 node ölümü):
  FSM BEKLEMEDE→PARKUR1→TAMAMLANDI, 4/4 waypoint varışı, Dosya-2 CSV
  (PARKUR1 satırlarında hiz_sp=1.000 + yon_sp dolu; BOOT/TAMAMLANDI boş =
  F-V.2 doğru), grafik CSV 1542 satır, Dosya-3 154 PNG, Ekran-2 PNG
  render (--thrust-birim %) göz teyitli.

----------------------------------------------------------------
BEKLEYENLER
----------------------------------------------------------------
- son_kod'un git push'u + Jetson'a deploy + servis kurulumu
- SERVO1=73 / SERVO3=74 kanal eşlemesi saha teyidi
- MicoAir frekans teyidi (2.4 GHz ise yasak)
- FC'ye WP_SPEED vb. parametrelerin yazılması
- Laptop hatırlatması: Jetson topic'leri için export ROS_DOMAIN_ID=42
================================================================
